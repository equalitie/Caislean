{% set base_dn = 'dc=' + domain_name|split('.')|join(',dc=') %}
server_host = localhost
server_port = 389
search_base = ou=mail,{{ base_dn }}
query_filter = (&(objectClass=mailAccount)(mail=%s))
result_attribute = maildrop
version = 3
bind = no
