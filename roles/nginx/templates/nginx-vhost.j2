server {
	listen	80;
	listen	[::]:80;
	listen	443 ssl;
	listen	[::]:443 ssl;

	server_name {{ item.name }};

	expires -1;

	root /var/www/{{ item.name }};

	include /etc/nginx/includes/{{ item.name }}/*;
}
