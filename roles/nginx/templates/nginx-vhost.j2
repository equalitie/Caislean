server {
	listen	80;
	listen	[::]:80;
	listen	443 ssl;
	listen	[::]:443 ssl;

	server_name {{ item }};

	expires -1;

	if ($request_method !~ ^(GET|HEAD|POST)$ )
	{
		return 405;
	}

	root /var/www/{{ item }};

	include /etc/nginx/includes/{{ item }}/*;
}
